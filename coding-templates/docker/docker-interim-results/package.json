{
  "name": "docker-interim-results",
  "version": "1.0.0",
  "description": "PlanQK coding template for NodeJS enables the creation of a PlanQK managed service, which is purely built based on the Dockerfile provided in the repository.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anaqor AG",
  "license": "Apache-2.0",
  "bin": {
    "sum": "index.js"
  }
}
