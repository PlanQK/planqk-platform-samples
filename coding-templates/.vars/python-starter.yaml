vars:
  - name: SERVICE_LOGIC_EXPLAINED
    value: |-
      The logic expects a `values` array with numbers (see [data.json](./input/data.json)) as input and returns the sum of the values as output.
      Further, it provides an optional flag to round down the sum, configurable through parameters (see [params.json](./input/params.json)).
